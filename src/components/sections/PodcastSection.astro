<section class="podcast-section">
  <div class="podcast-container">
    <div class="podcast-grid">
      <div class="podcast-art">
        <div class="podcast-cover">
          <div class="cover-grid"></div>
          <div class="cover-brand">
            <span class="cover-brand-light">Brick</span>
            <span class="cover-brand-bold">Wire<sup>&reg;</sup></span>
          </div>
          <div class="cover-title">No<br />CAP</div>
          <img src="/images/podcast-cover-art.webp" alt="" class="cover-bg" />
        </div>
      </div>
      <div class="podcast-content">
        <span class="podcast-eyebrow">PODCAST</span>
        <div class="podcast-divider"></div>
        <h2 class="podcast-title">No Cap by Brick Wire</h2>
        <p class="podcast-desc">
          Brick Wire is a weekly podcast offering an unfiltered look into
          commercial real estate's biggest trends and influential figures.
        </p>
        <div class="podcast-actions">
          <a href="/podcast/" class="podcast-btn">LISTEN NOW</a>
          <div class="podcast-platforms-row">
            <div class="podcast-platforms">
              <a
                href="#"
                target="_blank"
                rel="noopener noreferrer"
                aria-label="Listen on Spotify"
              >
                <svg width="32" height="32" viewBox="0 0 24 24" fill="#1DB954"
                  ><circle cx="12" cy="12" r="10"></circle><path
                    d="M6 9.5c4-2 10-1.5 13 .5M7.5 13c3-1.5 8-1 10.5.5M9 16.5c2-1 5.5-.5 7 .5"
                    stroke="white"
                    stroke-width="1.5"
                    fill="none"></path></svg
                >
              </a>
              <a
                href="#"
                target="_blank"
                rel="noopener noreferrer"
                aria-label="Listen on Apple Podcasts"
              >
                <svg width="32" height="32" viewBox="0 0 24 24" fill="#D56DFB"
                  ><circle cx="12" cy="12" r="10"></circle><path
                    d="M12 6a3 3 0 0 0-3 3v6a3 3 0 0 0 6 0V9a3 3 0 0 0-3-3z"
                    stroke="white"
                    stroke-width="1"
                    fill="none"></path></svg
                >
              </a>
              <a
                href="#"
                target="_blank"
                rel="noopener noreferrer"
                aria-label="Watch on YouTube"
              >
                <svg width="32" height="32" viewBox="0 0 24 24" fill="#FF0000"
                  ><circle cx="12" cy="12" r="10"></circle><path
                    d="M10 8.5v7l6-3.5l-6-3.5z"
                    fill="white"></path></svg
                >
              </a>
            </div>
            <span class="podcast-platforms-text"
              >Listen on Spotify, Apple Podcast and Youtube.</span
            >
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .podcast-section {
    background: var(--color-bg-dark);
    padding: 5rem 2rem;
  }
  .podcast-container {
    max-width: 1152px;
    margin: 0 auto;
  }
  .podcast-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }
  .podcast-art {
    display: flex;
    justify-content: center;
  }
  .podcast-cover {
    width: 100%;
    max-width: 480px;
    aspect-ratio: 1 / 1;
    border-radius: 1rem;
    overflow: hidden;
    position: relative;
    background: linear-gradient(
      180deg,
      var(--color-accent-light) 0%,
      var(--color-accent) 100%
    );
  }
  .cover-bg {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    opacity: 0.35;
    z-index: 1;
  }
  .podcast-cover::after {
    content: '';
    position: absolute;
    inset: 0;
    z-index: 1;
    background: radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.3) 100%);
    pointer-events: none;
  }
  .cover-grid {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(255, 255, 255, 0.14) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255, 255, 255, 0.14) 1px, transparent 1px);
    background-size: 32px 32px;
    pointer-events: none;
    z-index: 2;
  }
  .cover-brand {
    position: absolute;
    top: 2rem;
    left: 50%;
    transform: translateX(-50%);
    z-index: 2;
    font-size: 0.875rem;
    text-align: center;
  }
  .cover-brand-light {
    font-family: var(--font-body);
    font-weight: 400;
    color: rgba(255, 255, 255, 0.9);
  }
  .cover-brand-bold {
    font-family: var(--font-body);
    font-weight: 700;
    color: white;
  }
  .cover-brand-bold sup {
    font-size: 0.5em;
    top: -0.4em;
  }
  .cover-title {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    z-index: 2;
    font-family: var(--font-heading);
    font-size: 5rem;
    font-weight: 900;
    letter-spacing: -0.03em;
    line-height: 0.95;
    color: white;
    text-align: center;
  }
  .podcast-content {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
  }
  .podcast-eyebrow {
    font-family: var(--font-body);
    font-size: 0.8125rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: rgba(255, 255, 255, 0.5);
    display: block;
  }
  .podcast-divider {
    width: 4rem;
    height: 1px;
    background: rgba(255, 255, 255, 0.2);
    margin-top: 0.5rem;
    margin-bottom: 1.5rem;
  }
  .podcast-title {
    font-family: var(--font-heading);
    font-size: 3rem;
    font-weight: 800;
    color: white;
    line-height: 1.1;
    margin: 0 0 1rem 0;
  }
  .podcast-desc {
    font-size: 1rem;
    color: rgba(255, 255, 255, 0.65);
    line-height: 1.7;
    margin: 0 0 2rem 0;
    max-width: 480px;
  }
  .podcast-actions {
    display: flex;
    align-items: center;
    gap: 1.5rem;
    flex-wrap: wrap;
  }
  .podcast-btn {
    display: inline-block;
    background: var(--color-accent);
    color: white;
    font-weight: 700;
    font-size: 0.8125rem;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    padding: 1rem 2.5rem;
    border: none;
    cursor: pointer;
    text-decoration: none;
    transition: background var(--transition-fast);
  }
  .podcast-btn:hover {
    background: var(--color-accent-hover);
  }
  .podcast-platforms-row {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    flex-wrap: wrap;
  }
  .podcast-platforms {
    display: flex;
    gap: 0.5rem;
  }
  .podcast-platforms a {
    display: flex;
    transition: transform var(--transition-fast);
  }
  .podcast-platforms a:hover {
    transform: scale(1.1);
  }
  .podcast-platforms a svg {
    width: 28px;
    height: 28px;
  }
  .podcast-platforms-text {
    font-size: 0.75rem;
    color: rgba(255, 255, 255, 0.45);
    white-space: nowrap;
  }
  @media (max-width: 48em) {
    .podcast-section {
      padding: 3rem 1.25rem;
    }
    .podcast-grid {
      grid-template-columns: 1fr;
      gap: 2.5rem;
    }
    .podcast-art {
      order: -1;
    }
    .podcast-cover {
      max-width: 360px;
    }
    .cover-title {
      font-size: 3.5rem;
    }
    .cover-brand {
      top: 1.25rem;
    }
    .podcast-content {
      text-align: center;
      align-items: center;
    }
    .podcast-title {
      font-size: 2.25rem;
    }
    .podcast-desc {
      max-width: 100%;
    }
    .podcast-actions {
      justify-content: center;
    }
    .podcast-divider {
      margin-left: auto;
      margin-right: auto;
    }
  }
</style>
